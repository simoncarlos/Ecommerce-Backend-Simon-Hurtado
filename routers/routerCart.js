import { Router } from "express"

const cart = new Router()

// controllers

//cart.post("/", controller) // crea carrito y devuelve id
//
//cart.delete("/:id_cart", controller) // vacia un carrito
//
//cart.post("/:id_cart/products", controller) // añade producto por id que se envia en la request
//
//cart.get("/:id_cart/products") // lista productos de carrito
//
//cart.delete("/:id_cart/products/:id_product") // elimina producto del carrito


export default cart